# Test HTTP Module
# Verifies http.gr is functional and accessible

import "http"

print("=== Testing HTTP Module ===")
print("")

# Note: We can't make real HTTP requests in tests easily
# Just verify the module imports successfully
print("Testing HTTP module import...")
print("  Module imported successfully!")

# Test parse_url (helper function that doesn't need network)
print("")
print("Testing parse_url()...")
print("  Parsing: 'http://example.com:8080/path?query=value'")
print("  Result: " + http.parse_url("http://example.com:8080/path?query=value").to_string())

print("")
print("=== HTTP Module: ALL TESTS PASSED ✓ ===")
