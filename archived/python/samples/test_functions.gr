# Test basic function functionality

# Simple function declaration
func greet(name) {
    return "Hello, " + name + "!"
}

# Call the function
message = greet("World")
print message

# Function with multiple parameters
func add(x, y) {
    return x + y
}

result = add(15, 27)
print "15 + 27 = " + result.to_string()

# Function without return (should return none)
func say_hello() {
    print "Hello from function!"
}

say_hello()