# Test for-loop with Value system
items = [10, 20, 30]
sum = 0

for item in items {
    sum = sum + item
}

# Test list operations
numbers = []
for i in [1, 2, 3, 4, 5] {
    numbers.append(i * 2)
}

# Test map operations
config = {}
config["host"] = "localhost"
config["port"] = 8080

# Return results
sum