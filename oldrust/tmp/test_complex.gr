# Test variables
x = 10
y = 20
result = x + y

# Test strings
greeting = "Hello"
name = "World"
message = greeting + " " + name

# Test lists
items = [1, 2, 3]
items.append(4)

# Test functions
func double(n) {
    return n * 2
}

doubled = double(21)

# Output results
result
message
items
doubled