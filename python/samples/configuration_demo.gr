# Configuration Blocks Demo
# Demonstrates Glang's explicit behavior configuration system

string title = "Configuration Blocks Demonstration"
print("=== " + title + " ===")

# Basic configuration block with body
configure { mode: "development" } {
    string environment = "dev server"
    print("Running in: " + environment)
}

print("Configuration demo completed")

# Note: Configuration blocks currently parse correctly and execute
# Future phases will implement actual behavior enforcement